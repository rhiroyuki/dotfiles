# Enable DRM kernel mode setting for the NVIDIA driver.
# Required for a clean Wayland session on NVIDIA (Hyprland/Sway). The 580 driver
# defaults modeset on, but setting it explicitly keeps it pinned across driver
# changes. Installed to /etc/modprobe.d/ by install/install_nvidia_modeset.sh.
options nvidia_drm modeset=1
