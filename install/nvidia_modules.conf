# Managed by dotfiles: keep NVIDIA DRM modules in every mkinitcpio image.
# mkinitcpio reads this file after /etc/mkinitcpio.conf, so the main config
# remains untouched and its existing MODULES entries are preserved.
MODULES+=(nvidia nvidia_modeset nvidia_uvm nvidia_drm)
